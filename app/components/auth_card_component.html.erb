<div class="space-y-4 md:space-y-6 max-w-screen-xl mx-auto">
  <% if @header %>
  <header>
    <h1 class="mb-5 text-xl font-bold leading-tight tracking-tight text-gray-600 md:text-2xl"><%= @header %></h1>
  </header>
  <% end %>
  <%= content %>
</div>